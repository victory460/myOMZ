import { SimulatedDeviceHelperService } from '../simulated-device-helper.service';
export declare class SimulatedDcService {
    private simulatedDeviceService;
    private voltages;
    constructor(_simulatedDeviceService: SimulatedDeviceHelperService);
    getVoltage(_chan: any): {
        command: string;
        voltage: number;
        statusCode: number;
        wait: number;
    };
    setVoltage(_chan: any, _voltage: any): {
        command: string;
        statusCode: number;
        wait: number;
    };
}
