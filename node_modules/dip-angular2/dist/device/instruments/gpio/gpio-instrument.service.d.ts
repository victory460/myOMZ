import { Observable } from 'rxjs/Observable';
import 'rxjs/Rx';
import { GenericInstrumentService } from '../generic-instrument.service';
import { TransportContainerService } from '../../../transport/transport-container.service';
export declare class GpioInstrumentService extends GenericInstrumentService {
    readonly numChans: number;
    readonly sinkCurrentMax: number;
    readonly sourceCurrentMax: number;
    constructor(_transport: TransportContainerService, _gpioInstrumentDescriptor: any);
    setParameters(chans: Array<number>, directions: string[]): Observable<any>;
    write(chans: Array<number>, values: Array<number>): Observable<any>;
    read(chans: Array<number>): any;
}
