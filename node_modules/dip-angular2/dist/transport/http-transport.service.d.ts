import { Observable } from 'rxjs/Observable';
import 'rxjs/Rx';
import { GenericTransportService } from './generic-transport.service';
export declare class HttpTransportService extends GenericTransportService {
    private streamState;
    private start;
    private finish;
    private timeout;
    private timeoutMinMs;
    private timeoutMaxMs;
    constructor(_rootUri: string, timeout: number);
    getUri(): string;
    setTimeout(newTimeout: number): void;
    private forceRange(val, min, max);
    getRequest(requestUrl: string, timeout?: number): Observable<any>;
    writeRead(endpoint: string, sendData: any, dataType: string, timeout?: number): Observable<any>;
    writeReadHelper(rootUri: string, endpoint: string, sendData: any, dataType: string, timeout?: number): Observable<any>;
    streamFrom(endpoint: string, sendData: any, dataType: string, delay?: number): Observable<any>;
    stopStream(): void;
    getType(): string;
}
